# Luma API Configuration
LUMA_API_KEY=your_luma_api_key_here
LUMA_API_BASE_URL=https://public-api.luma.com

# Use mock data instead of real API (set to true for demo/testing without API key)
# When enabled, calendarId parameter is ignored and mock data is returned
USE_MOCK_DATA=false

# Server Configuration
PORT=4000
NODE_ENV=development
