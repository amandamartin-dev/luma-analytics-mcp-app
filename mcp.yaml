transport:
  type: streamable_http
  port: 8000

endpoint: http://localhost:4000

schema:
  source: local
  path: supergraph-schema.graphql

# Enable MCP Apps specification 
ui_apps:
  specs: ["mcp-apps"]

introspection:
  introspect:
    enabled: true